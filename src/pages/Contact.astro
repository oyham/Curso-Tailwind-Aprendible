---
import Layout from "../layouts/Layout.astro";
---

<Layout title="Contacto">
  <main class="p-4 mx-auto max-w-screen-lg text-center md:pt-20 2xl:text-3xl">
    <h1 class="text-4xl font-bold mb-8">Contacto</h1>

    <section class="mb-8">
      <h2 class="text-2xl font-bold mb-4">Información de Contacto</h2>
      <p class="text-lg mb-4">
        Puedes ponerte en contacto con nosotros a través de los siguientes
        medios:
      </p>
      <div class="flex justify-center">
        <ul class="list-disc pl-4 text-justify">
          <li class="mb-2">Correo Electrónico: info@example.com</li>
          <li class="mb-2">Teléfono: +123 456 789</li>
          <li class="mb-2">Dirección: Calle Principal, Ciudad</li>
        </ul>
      </div>
    </section>

    <section class="2xl:text-4xl">
      <h2 class="text-2xl font-bold mb-4">Formulario de Contacto</h2>
      <form class="max-w-screen-md mx-auto 2xl:max-w-screen-2xl">
        <div class="mb-4">
          <label
            for="name"
            class="block text-sm font-medium text-gray-700 2xl:text-3xl"
            >Nombre</label
          >
          <input
            type="text"
            id="name"
            name="name"
            class="mt-1 p-2 w-full border rounded-md"
          />
        </div>
        <div class="mb-4">
          <label
            for="email"
            class="block text-sm font-medium text-gray-700 2xl:text-3xl"
            >Correo Electrónico</label
          >
          <input
            type="email"
            id="email"
            name="email"
            class="mt-1 p-2 w-full border rounded-md"
          />
        </div>
        <div class="mb-4">
          <label
            for="message"
            class="block text-sm font-medium text-gray-700 2xl:text-3xl"
            >Mensaje</label
          >
          <textarea
            id="message"
            name="message"
            rows="4"
            class="mt-1 p-2 w-full border rounded-md"></textarea>
        </div>
        <button
          type="submit"
          class="bg-blue-500 text-white px-4 py-2 rounded-md"
          >Enviar Mensaje</button
        >
      </form>
    </section>
  </main>
</Layout>
